package array;

public class ArrayDocs {
	
	/*
	 * ================= Array ==============
	 * 
	 * # collection of homogeneous data.
	 * # collection/group of multiple values of same data type.
	 * # it has a fixed size.
	 * # data store in the basis of indices(i.e. index starts from 0)
	 * # contains duplicate data
	 * # random access
	 * # contiguous memory allocation
	 * 
	 *  syntax:
	 *   
	 *     data_type  array_name[] = new data_type[size];
	 *     
	 *     e.g.
	 *     
	 *     int marks[] = new int[200];
	 *     
	 *     String  cities[] = new String[50];
	 *     
	 *     char[]   alphas = new char[26];
	 *     
	 *     int   []salary = new int[500];
	 *     
	 *     float[]   price = new float[2000];
	 *     
	 *    // Book books[] = new Book[5000];
	 *     
	 */

}
